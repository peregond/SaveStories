<Page
    x:Class="SaveMe.WinUI.Beta.Pages.ReelsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="24,16,24,24" ColumnSpacing="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <ScrollViewer HorizontalScrollMode="Disabled" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Spacing="12">
                <Border Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8">
                    <StackPanel Spacing="8">
                        <TextBlock FontSize="20" FontWeight="SemiBold" Text="Reels" />
                        <TextBlock Opacity="0.75" Text="Скачать Reels по ссылке" />
                    </StackPanel>
                </Border>

                <Border Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8">
                    <StackPanel Spacing="10">
                        <TextBlock FontWeight="SemiBold" Text="Ссылки на Reels" />
                        <TextBox
                            x:Name="ReelsInputTextBox"
                            AcceptsReturn="True"
                            MinHeight="160"
                            PlaceholderText="Вставь ссылки на Reels, по одной на строку." />
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button Click="OnAddLinksClick" Content="Добавить" />
                            <Button Click="OnClearInputClick" Content="Очистить" />
                            <Button Click="OnCheckSessionClick" Content="Проверить сессию" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button x:Name="RunReelsButton" Click="OnDownloadClick" Content="Скачать" />
                            <Button x:Name="StopReelsButton" Click="OnStopClick" Content="Остановить" IsEnabled="False" />
                            <Button x:Name="RetryFailedReelsButton" Click="OnRetryFailedClick" Content="Повторить неудачные" IsEnabled="False" />
                        </StackPanel>
                        <TextBlock x:Name="ReelsQueueSummaryText" Opacity="0.75" Text="Очередь: 0 ссылок." />
                        <ListView x:Name="ReelsQueueListView" MaxHeight="160" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <ScrollViewer Grid.Column="1" HorizontalScrollMode="Disabled" HorizontalScrollBarVisibility="Disabled">
            <Border
                Padding="16"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8">
                <StackPanel Spacing="10">
                    <TextBlock FontWeight="SemiBold" Text="Состояние" />
                    <TextBlock x:Name="ReelsStatusTitleText" Text="Ожидание" />
                    <TextBlock x:Name="ReelsStatusDetailText" Opacity="0.75" Text="Готово к запуску выгрузки Reels." />

                    <TextBlock FontWeight="SemiBold" Text="Результат" />
                    <TextBlock x:Name="ReelsResultSummaryText" Opacity="0.75" Text="Ссылок: 0  ·  Сохранено: 0  ·  Файлов: 0" />

                    <TextBlock FontWeight="SemiBold" Text="Последние загрузки" />
                    <ListView x:Name="ReelsDownloadsListView" MaxHeight="140" />

                    <TextBlock FontWeight="SemiBold" Text="Логи" />
                    <TextBox
                        x:Name="ReelsLogsTextBox"
                        AcceptsReturn="True"
                        MinHeight="180"
                        IsReadOnly="True"
                        TextWrapping="Wrap" />
                </StackPanel>
            </Border>
        </ScrollViewer>
    </Grid>
</Page>
