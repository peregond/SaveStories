name: Build WinUI Beta

on:
  workflow_dispatch:
  push:
    tags:
      - "winui-beta-v*"
    paths:
      - "windows_app_winui_beta/**"
      - ".github/workflows/winui-beta-build.yml"

permissions:
  contents: write

jobs:
  build-winui-beta:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 8 SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Restore
        shell: pwsh
        run: |
          dotnet restore .\windows_app_winui_beta\SaveStories.WinUI.Beta.sln

      - name: Build
        shell: pwsh
        run: |
          dotnet build .\windows_app_winui_beta\SaveStories.WinUI.Beta.sln -c Release -p:Platform=x64

      - name: Publish unpackaged beta app
        shell: pwsh
        run: |
          $outDir = "dist\winui-beta"
          if (Test-Path $outDir) {
            Remove-Item $outDir -Force -Recurse
          }
          dotnet publish .\windows_app_winui_beta\src\SaveStories.WinUI.Beta\SaveStories.WinUI.Beta.csproj `
            -c Release `
            -r win-x64 `
            --self-contained true `
            -p:WindowsPackageType=None `
            -p:WindowsAppSDKSelfContained=true `
            -o $outDir

      - name: Bundle Node.js runtime
        shell: pwsh
        run: |
          $nodeVersion = "v24.11.0"
          $nodeZip = "node-$nodeVersion-win-x64.zip"
          $downloadUrl = "https://nodejs.org/dist/$nodeVersion/$nodeZip"
          $zipPath = Join-Path $env:RUNNER_TEMP $nodeZip
          $extractRoot = Join-Path $env:RUNNER_TEMP "node-runtime"
          $targetDir = "dist\winui-beta\runtime\node"

          if (Test-Path $extractRoot) {
            Remove-Item $extractRoot -Recurse -Force
          }
          if (Test-Path $targetDir) {
            Remove-Item $targetDir -Recurse -Force
          }

          Invoke-WebRequest -Uri $downloadUrl -OutFile $zipPath
          Expand-Archive -Path $zipPath -DestinationPath $extractRoot -Force
          $nodeRoot = Get-ChildItem -Path $extractRoot -Directory | Select-Object -First 1
          if (-not $nodeRoot) {
            throw "Unable to extract Node.js runtime."
          }
          New-Item -ItemType Directory -Path $targetDir -Force | Out-Null
          Copy-Item -Path (Join-Path $nodeRoot.FullName "*") -Destination $targetDir -Recurse -Force

      - name: Install Inno Setup
        shell: pwsh
        run: |
          if (Get-Command ISCC.exe -ErrorAction SilentlyContinue) {
            Write-Host "Inno Setup already available."
          } else {
            choco install innosetup -y --no-progress
          }

      - name: Build WinUI beta installer EXE
        shell: pwsh
        run: |
          $version = (Get-Content VERSION -Raw).Trim()
          $sourceDir = (Resolve-Path "dist\winui-beta").Path
          $outputDir = (Resolve-Path "dist").Path
          $baseName = "SaveStories-WinUI-Beta-Setup-v$version"
          $env:SAVESTORIES_VERSION = $version
          & ISCC.exe `
            "/DSourceDir=$sourceDir" `
            "/DOutputDir=$outputDir" `
            "/DOutputBaseFilename=$baseName" `
            ".\windows_app_winui_beta\SaveStories-WinUI-Beta.iss"
          if ($LASTEXITCODE -ne 0) {
            throw "ISCC failed with code $LASTEXITCODE"
          }

      - name: Upload beta installer artifact
        uses: actions/upload-artifact@v4
        with:
          name: SaveStories-WinUI-Beta-Setup
          path: dist/SaveStories-WinUI-Beta-Setup-v*.exe

      - name: Upload beta installer to GitHub prerelease
        if: startsWith(github.ref, 'refs/tags/winui-beta-v')
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $tag = "${{ github.ref_name }}"
          $setup = Get-ChildItem -Path "dist" -Filter "SaveStories-WinUI-Beta-Setup-v*.exe" -File | Sort-Object LastWriteTime -Descending | Select-Object -First 1
          if (-not $setup) {
            throw "Beta installer not found for tag: $tag"
          }
          if (gh release view $tag *> $null) {
            gh release upload $tag $setup.FullName --clobber
          } else {
            gh release create $tag $setup.FullName --title "SaveStories WinUI Beta $tag" --prerelease --notes "Automated WinUI beta build."
          }
